- if flash[:error]
  %section.bounded
    .three-quarter-container.bounded.no-border.dark
      = flash[:error]

%section.bounded
  .three-quarter-container.bounded
    %h3.ribbon-header Add New Payment

    = render :partial => 'form', :locals => { :payment => @payment, :people => @people, :person => @person }
