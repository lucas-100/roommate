

 
  
  <div class="clearfix"></div>
  
  <div id="header">
    <h1><%= @house.name %></h1>
    <%= link_to '', new_house_expense_path(@house), {:id => 'newExpense'}%>
    <a href="#" id="newPayment"><span>New Payment</span></a>
  </div>
  <div class="clearfix"></div>
  
  <div id="owe">
    <div id="oweTop"></div>
    <div id="oweMiddle">
        
        <div class="column_1">
        
          <ul>

            <% @person.all_debts_owed.each do |d| %>
            	<li><b><%= d[:person] %></b>: <%= d[:amount] %></li>
            <% end %>

          </ul>
        </div>
        
        <div class="column_2">
          <ul>

            <% @person.all_debts_loaned.each do |d| %>
            	<li><%= d[:person] %></b>: <%= d[:amount] %></li>
            <% end %>

          </ul>
        </div>
    
      <div class="clearfix"></div>
    </div>
    <div id="oweBottom"></div>
  </div>
  
  <div id="expenses">
    <a class="addBanner" href="#"><span>Add Expenses</span></a>
    <div id="expensesHeader" class="boxHeader"></div>
    <div id="roommatesMiddle" class="boxMiddle">
      <div class="column_1">
        <h2>Things I didn't pay for</h2>
        <ul>
          <% @person.recent_expenses.each do |e| %>
            <li><%= "#{e.name} -- #{e.amount}" %></li>
          <% end %>
        </ul>
      </div>
      <div class="column_2">
        <h2>Things I paid for</h2>
        <ul>
          <% @person.recent_loans.each do |e| %>
            <li><%= "#{e.name} -- #{e.amount}" %></li>
          <% end %>
        </ul>
      </div>
    </div>
    <div id="roommatesFooter" class="boxFooter"></div>
  </div>
  
  <div id="roommates">
    <%= link_to '', new_house_person_path(@house),  {:class => 'addBanner'}%>
    <div id="roommatesHeader" class="boxHeader"></div>
    <div id="roommatesMiddle" class="boxMiddle">
      <ul>
        <% for roommate in @house.people %>
          <li><%= roommate.name %></li>
        <% end %>
      </ul>
    </div>
    <div id="roommatesFooter" class="boxFooter"></div>
  </div>
  
  <div class="clearfix"></div>
  